import * as React from "react";
import {StateService} from '../services/StateService'

export class HandVisual extends React.Component {
    stateService: StateService = StateService.instance

    render() {
        return (
            <div className="">
            </div>
        )
    }
}