export type TableInfoProps = {
    showRoundInfo?: boolean
    showTableNumber?: boolean
    showTimer?: boolean
    gamesLeft?: number
    round: string
    honbaCount: number
    riichiCount: number
    currentTime?: string
    tableNumber?: number
}